## 1
add radius for right angle

## 2
fix dark mode right angle, should also add radius

## 3
fix the horizontal scrolling bug by removing 'overflow: hidden' from .table-container and ensuring 'overflow-x: auto' is used instead. Also ensure border-radius works with scrolling.

## 4
Add a watcher for isDarkMode to toggle the 'dark-mode' class on the document body, ensuring the entire page background matches the dark theme. Also add a border-radius to the #app container to ensure any visible boundaries are rounded.

## 5
Ensure dark-mode class is applied to the body on mount. Update styles to round the corners of the #app container and set a background on the body in dark mode to avoid sharp edges.

## 6
make it more flat

## 7
restore last step, I mean make the row of github icon not so high

## 8
add github.icon at bottom center, and link it to https://github.com/kxw07/weekly-time-management when click
